{
  "name": "zotero-plus",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build:addon": "npm run build -w zotero-plus-bridge-addon",
    "test": "npm --workspaces --if-present test"
  }
}
